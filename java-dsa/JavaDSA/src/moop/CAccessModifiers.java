package moop;
/*
Access Modifiers: who all can access , this helps in providing restriction

public
	can be accessed any where in the same class, other class in same and different package
	
private
	can be accessed only in the class where it is defined
	
	
default	
	if we donot specify any modifier then it is default modifier
	which can be accessed only in the same package
	we cannot access it in other package
	
protected
	can be accessed in the same package and also in the other packages in which the class
	they were defined is inherited then it can be accessed
 */
public class CAccessModifiers {

	private String name;
}
